spring:
  application:
    name: webflux-server
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://127.0.0.1/webflux}

server:
  port: 8080

logging:
  level:
    root: INFO

error-controler-advice:
  enabled: false

webflux-server:
  base-path: /api
  github:
    url: https://api.github.com
